'use strict';

/**
 * categoria-accesorio controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::categoria-accesorio.categoria-accesorio');
